"""LoRA collection: an approved skill spec, a teacher LLM, a dataset skill on disk.

A spec is written by hand or proposed by the harness pattern miner. Nothing is collected from a
spec that a person has not marked approved.
"""
