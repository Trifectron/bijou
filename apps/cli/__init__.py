"""The developer console: a modal terminal UI over the just recipes.

units lists what can run, runner starts and stops it, logs keeps and mirrors its
output, status gathers what the status bar shows, and app draws all of it.
"""
