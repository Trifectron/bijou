[project]
name = "cli"
version = "0.1.0"
description = "The developer console: every just recipe, its logs, and the state of the repo"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "pydantic>=2.10",
    "pydantic-settings>=2.7",
    "rich>=13",
    "textual>=6",
]

[project.scripts]
console = "cli.app:run"

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = { "cli" = "." }
packages = ["cli", "cli.core"]
